export const saveToVault = async (id: string, data: any): Promise<void> => {
  // Stub: save to vault
};

export const loadFromVault = async (id: string): Promise<string | null> => {
  // Stub: load from vault
  return null;
};

export const getStoredBytes = async (): Promise<number> => {
  // Stub: return stored bytes
  return 0;
};
