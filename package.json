{
  "name": "bitbeats-p2p",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "build": "npx prisma generate && tsc && cd client && npm run build",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "express": "^4.19.2",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "prisma": "^5.19.1",
    "webtorrent-hybrid": "^4.1.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}